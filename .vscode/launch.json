{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm run start:dev",
      "name": "Run start:dev",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceRoot}"
      // "envFile": "${workspaceFolder}/.env.development"
    }
  ]
}
